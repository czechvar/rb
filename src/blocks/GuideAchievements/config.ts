import type { Block } from 'payload'

export const GuideAchievementsBlockConfig: Block = {
  slug: 'guideAchievements',
  labels: { singular: 'Guide Achievements', plural: 'Guide Achievements blocks' },
  fields: [],
}

export const GuideAchievementsBlock = GuideAchievementsBlockConfig
