import type { Block } from 'payload'

export const ProgramWeeksBlockConfig: Block = {
  slug: 'programWeeks',
  labels: { singular: 'Program Weeks', plural: 'Program Weeks blocks' },
  fields: [],
}

export const ProgramWeeksBlock = ProgramWeeksBlockConfig
