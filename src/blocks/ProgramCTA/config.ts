import type { Block } from 'payload'

export const ProgramCTABlockConfig: Block = {
  slug: 'programCTA',
  labels: { singular: 'Program CTA', plural: 'Program CTA blocks' },
  fields: [],
}

export const ProgramCTABlock = ProgramCTABlockConfig
