import type { Block } from 'payload'

export const HomeTeamBlockConfig: Block = {
  slug: 'homeTeam',
  labels: { singular: 'Home Team', plural: 'Home Team' },
  fields: [],
}

export const HomeTeamBlock = HomeTeamBlockConfig
