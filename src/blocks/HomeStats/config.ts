import type { Block } from 'payload'

export const HomeStatsBlockConfig: Block = {
  slug: 'homeStats',
  labels: { singular: 'Home Stats', plural: 'Home Stats' },
  fields: [],
}

export const HomeStatsBlock = HomeStatsBlockConfig
