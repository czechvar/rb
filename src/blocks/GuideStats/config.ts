import type { Block } from 'payload'

export const GuideStatsBlockConfig: Block = {
  slug: 'guideStats',
  labels: { singular: 'Guide Stats', plural: 'Guide Stats blocks' },
  fields: [],
}

export const GuideStatsBlock = GuideStatsBlockConfig
