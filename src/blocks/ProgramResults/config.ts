import type { Block } from 'payload'

export const ProgramResultsBlockConfig: Block = {
  slug: 'programResults',
  labels: { singular: 'Program Results', plural: 'Program Results blocks' },
  fields: [],
}

export const ProgramResultsBlock = ProgramResultsBlockConfig
