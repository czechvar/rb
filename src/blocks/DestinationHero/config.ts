import type { Block } from 'payload'

export const DestinationHeroBlockConfig: Block = {
  slug: 'destinationHero',
  labels: { singular: 'Destination Hero', plural: 'Destination Hero blocks' },
  fields: [],
}

export const DestinationHeroBlock = DestinationHeroBlockConfig
