import type { Block } from 'payload'

export const HomeHeroBlockConfig: Block = {
  slug: 'homeHero',
  labels: { singular: 'Home Hero', plural: 'Home Heroes' },
  fields: [],
}

export const HomeHeroBlock = HomeHeroBlockConfig
