import type { Block } from 'payload'

export const HomeDestinationsBlockConfig: Block = {
  slug: 'homeDestinations',
  labels: { singular: 'Home Destinations', plural: 'Home Destinations' },
  fields: [],
}

export const HomeDestinationsBlock = HomeDestinationsBlockConfig
