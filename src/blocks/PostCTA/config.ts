import type { Block } from 'payload'

export const PostCTABlockConfig: Block = {
  slug: 'postCTA',
  labels: { singular: 'Post CTA', plural: 'Post CTAs' },
  fields: [],
}

export const PostCTABlock = PostCTABlockConfig
