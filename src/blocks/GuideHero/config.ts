import type { Block } from 'payload'

export const GuideHeroBlockConfig: Block = {
  slug: 'guideHero',
  labels: { singular: 'Guide Hero', plural: 'Guide Heroes' },
  fields: [],
}

export const GuideHeroBlock = GuideHeroBlockConfig
